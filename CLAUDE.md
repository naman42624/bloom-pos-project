# CLAUDE.md — Petal (flower shop operations app)

This file loads automatically every session. It exists so decisions made once don't have to be re-argued or re-discovered from scratch. If you (Claude) get corrected on the same thing twice, add a line here rather than relying on conversation memory.

## What this is

An internal operations app for a flower shop: multi-employee, multi-channel orders (WhatsApp, website, email, walk-in), in-house delivery. **It is live and in daily use at the counter** — this is not a greenfield project. Currently mid-redesign: fixing order flow, staff/employee identity, cash register, settlements, and attendance, which are inconsistent/broken in practice despite being previously marked "complete" (`PROGRESS.md` tracked 241/270 tasks across 10 phases before this redesign started).

## Stack — do not change without explicit user approval

- **Backend**: Express.js + JWT auth + PostgreSQL via `pg` (`server/`).
- **Frontend**: Expo (React Native) + React Navigation (not Expo Router). Targets iOS, Android, web from one codebase.
- Two reference docs were consulted for ideas: `/Users/gauravbhatia/Downloads/files/petal-prd.md` and `build-plan.md`. Their **concepts** are being adapted (unified order inbox, channel tag, buyer≠recipient, cost/margin separation, PIN quick-switch). Their **stack** (Supabase, Row-Level Security, Expo Router, TypeScript, NativeWind, offline-first sync) is explicitly NOT being adopted — this project stays on Express/JWT/pg. Don't suggest migrating to Supabase/RLS.

## Non-negotiable constraints

1. **Live production data — never lose it.** Every schema change is additive-only: new columns, new tables. Never `DROP`/`RENAME`/restructure a table or column already holding live data without an explicit, separately-approved migration plan with a backup taken first. Read the `db-migration-safety` skill before writing any migration touching `server/config/database.js` or any SQL migration file.
2. **Users are non-technical, first-time users of any business software.** Counter staff, florists, and riders have never used an app like this before. Every staff-facing flow must be obvious with zero training. Read the `staff-ux-checklist` skill before finalizing any staff-facing screen or flow design.
3. **No offline-first requirement.** Shop connectivity is reliable — don't build a local-write-queue/background-sync system; a simple retry/error message on a dropped request is sufficient.
4. **Customer self-ordering stays in scope.** Do not remove `CustomerShopScreen.js` or the `customer` role — the user explicitly chose to keep this, diverging from the reference PRD's staff-only stance.
5. **Approach is repair-in-place, not a rewrite.** Reuse working modules (inventory, products/QR, POS cart fundamentals) rather than rebuilding them. See the project memory file for the full decision log if more history is needed.

## Target role model (migrating to this from today's single generic "employee" role)

Owner · Manager · Sales/Counter Staff · Florist/Prep Staff · Delivery Rider · Customer.

- Manager: team-wide sales visibility; cost/margin data hidden by default (owner can toggle it on).
- Staff login: **employee code + PIN** on a shared counter device, fast identity switch, no OTP dependency. Owner/Manager keep phone + password on their own personal devices. (Today: no employee_code/PIN concept exists at all — everyone logs in with phone+password.)

## Locations

Single location today, expanding soon. Keep the data model multi-location-capable (it already is) but don't over-invest in multi-location UI polish yet.

## Order channels (real today, not aspirational)

WhatsApp, Website, Email, Walk-in/Phone. None need API integration — website orders are logged manually by staff exactly like WhatsApp/email. All four need a `channel` tag distinct from fulfillment type (`order_type`), which doesn't exist yet.

## UX design principles — every staff-facing screen, no exceptions

- Assume the person has never used a business app before in their life. No jargon, no hidden gestures, no multi-level menus for anything done daily.
- Big tap targets, large legible text, one clearly primary action per screen.
- The common-case path must be the *shortest* path. A plain counter sale target: well under 6 taps, well under 30 seconds.
- **Never cut functionality for the sake of simplicity.** Rare/advanced actions can live one level deeper (not deleted) — simple ≠ stripped-down.
- Prefer showing over asking: sensible defaults, pre-filled fields, remembered last choices (e.g. last channel used) over blank forms demanding input.
- Errors say what to do next in plain language ("Register isn't open — tap here to open it") never a technical message.
- See the `staff-ux-checklist` skill for the concrete pre-ship checklist to run any new/changed staff screen through.

## Roadmap — sub-projects, in priority order

Order-side work is first because the app is live at the counter and that's today's acute pain. Each sub-project gets its own brainstorm → design doc (`docs/superpowers/specs/YYYY-MM-DD-<topic>-design.md`) → implementation plan → build cycle. Update the status marker here as phases complete.

1. **Order model, channel & unified inbox** — done, merged to `main` (PRs #1 and #2)
2. **Identity, roles & employee code/PIN login** — done. PR #3 (`feature/identity-roles-pin-login`, not yet merged) now also carries sub-project 3's work below — built directly onto this branch (see note under item 3) rather than a separate PR, so #3 is the one PR covering both.
3. **POS/checkout integrity** — done, live-tested on `feature/identity-roles-pin-login`. Register-guard hard enforcement (`server/utils/register-guard.js`) is wired into sale creation, add-payment, refund, `PUT /:id/cancel` (redesigned to block cancelling a sale with an unrefunded balance instead of silently reversing register totals), pickup completion, and both delivery-settlement routes. `GET /api/expenses`/`GET /api/expenses/summary`/`GET /api/purchase-orders`/`GET /api/purchase-orders/:id` — previously `authenticate`-only, so `role='customer'` could read them — now `authorize('owner', 'manager', 'employee', 'counter_staff')`, matching each route's own existing write-access role list. Register-guard coverage was audited against the customer self-order endpoint and the recurring-order processor (both flagged as open questions, 2026-09-01): confirmed neither ever writes to `payments` or touches `cash_registers` — both only ever create a sale with `payment_status: 'pending'`, actual cash collection happens later through already-covered routes — so no fix was needed there, just verification. (The now-stale `feature/pos-checkout-integrity` branch + its stash `stash@{0}`, which once had an earlier draft of this same fix, are safe to delete — this work was redone fresh here instead.) `GET /api/sales` has the same lack of a role guard, deliberately accepted (order detail isn't sensitive the same way). One more gap found immediately after, same session: the backend authorize() fix alone didn't answer "how does counter_staff actually add an expense" — the only UI entry point into `ExpensesScreen` was a button on `CashRegisterScreen` shared under one `canManage`-only gate with the (correctly owner/manager-only) "View History" button. Split into two independently-gated buttons; `ExpensesScreen` itself has no internal role gate, so that was the whole fix.
   - **Deferred, in-session design proposal (not yet built):** the counter_staff dashboard's "Orders Needing Attention" list doesn't scale past ~20 orders — it's sorted `created_at DESC` (recency, not urgency) with no render cap, so at 100 orders it becomes a very long, unprioritized scroll. Proposed: an opt-in `sort=urgency` param on `GET /sales` (rush-flagged first, then earliest scheduled time, then oldest-pending — additive, doesn't change the existing default sort for other callers), the dashboard fetching a small capped page (~8) with that sort while still reading the already-computed `total` field for an accurate "Need Attention: N" stat, and the "+N more" link carrying the same status filter into Orders Inbox (which already has search/filters/a virtualized list — the right tool for browsing everything, not something to duplicate on the dashboard). User said to defer implementation, discuss again later.
4. **Cash register & settlement reconciliation** — largely done (2026-09-01), no formal spec doc (worked via exploratory audit + direct fixes, not the full brainstorm→design→plan cycle — a smaller sub-project than 1-3). Covered: an exhaustive live-tested audit of every register-touching flow (sale creation, refund, cancel, pickup, settlement, expenses — see "Known structural debt" for the 3 real bugs it found and fixed); counter_staff granted settlement create/verify/list permission (matches the shop's real workflow — counter staff, not owner/manager, physically take the cash handoff from a delivery partner); the pending-COD visibility gap fixed (was owner/manager-only despite the backend already allowing broader read access, now surfaced on the counter_staff/employee Dashboard too); the dead two-step settlement path (`POST /settlements` + `PUT /settlements/:id/verify`) deleted — `settle-now` was always the only path the frontend used; a real-time in-app notification the instant a delivery partner collects COD, reusing the existing `notifyByRole` helper; one cheap WhatsApp deep-link (`wa.me`) added to settlement partner cards to remind/confirm a handoff. **Still genuinely open, not a quick fix:** COD collected as "UPI" is entirely self-reported by the delivery partner (method + reference number, no verification against a bank/payment gateway) — closing this for real needs actual payment gateway integration, a materially bigger feature, explicitly flagged rather than silently left implied-solved. A full WhatsApp Business API integration (auto order-notifications to customers, replacing manual texting) was discussed and deliberately deferred — would reverse this file's existing "WhatsApp needs no API integration" decision, needs its own dedicated brainstorm if picked up.
   - **Continuation — register/checkout calculation-integrity sweep (2026-09-04):** triggered by two live user bug reports (a UPI-collected COD settlement still showing as cash discrepancy at close; QuickCheckoutScreen's "partial" payment mode collapsing a split cash+UPI advance into one cash entry), which exposed that the prior "exhaustive" audit above had tested settle-now's method-split and register-close's math only in isolation, never combined — the actual gap. Both root-caused and fixed: `sumCollectionsByMethod` (the cash/UPI-splitting logic) extracted from `deliveries.js` to `server/utils/settlement-math.js` so `register/close` uses the exact same calculation settle-now already did, instead of its own divergent copy that treated all COD settlements as cash; `QuickCheckoutScreen.js`'s 'partial' mode payment-entry construction unified with 'pay_now''s (previously hardcoded to `payments[0].method` + the full advance amount regardless of split state — worse than reported, since even the non-split method selector was silently ignored too). A user-invited subagent audit of the surrounding money code then found three more real bugs of the same "recomputed/replaced in one place without touching the register the way every sibling route does" shape, all fixed same session: `PUT /sales/:id` (Edit Sale)'s payment-method-edit path did a raw delete+reinsert of `payments` with **zero** `cash_registers` interaction and no open-register check at all (confirmed already exercised live 5 times via `audit_logs`, e.g. sale 126's cash→upi→card→upi edits) — fixed to compute the old-vs-new per-method delta and credit only that delta to the currently-open register, block cash edits when none is open, and preserve the earliest original payment's `created_at` on reinsert (rewriting it to "now" would let a payment collected in an already-closed earlier session get silently re-summed into a later session's close); `POST /customers/:id/credits` was the one remaining cash-write route with no `hasOpenRegister` guard (a cash dues payoff with the register closed was recorded and reduced the customer's balance but the cash never reached any register) — fixed with the same guard every sibling route already has. A fourth subagent finding (`POST /:id/refund` letting a single `refund_method` apply to a multi-method-paid sale) was investigated and **rejected as correct-by-design, not a bug**: a cash refund correctly debits `expected_cash` by the full refunded amount regardless of the sale's original payment mix, because that's real physical cash leaving the drawer now — refund method is deliberately independent of original payment method. `QuickCheckoutScreen.js`'s 'pay_now' split mode also got the same split-sums-to-total validation 'partial' mode already had (was only checked `>0`, silently accepting a mistyped over/under split). Separately investigated and resolved as **not a dead end, but a real minor gap**: a partially-collected COD (`/deliver` can only ever be called once — status moves to 'delivered' immediately) has no way to top up the remainder except the generic `POST /:id/payments`, which correctly resolves `payment_status`/`cod_status`/register totals but never updates `deliveries.cod_collected` — left as-is (no money-safety impact, just a stale "Collected: ₹X" figure on `DeliveryDetailScreen` afterward), not worth the schema/semantics cost of fixing given `cod_collected`'s intended meaning is "held by the delivery partner specifically," which a counter-collected top-up correctly shouldn't inflate. All fixes have permanent regression checks (`verify-order-flows.js`, `verify-register-expenses.js`); full 3-suite regression (65 checks: order-flows, register-expenses, identity-roles) passes clean. Not investigated/fixed this pass, flagged only: `DashboardScreenV2.js`'s `handleCollectPayment` has no upper-bound check against the due amount (lower priority — this is the older of the two coexisting dashboards, see "Known structural debt").
5. **Task assignment & delivery workflow** — done (2026-09-01), audit-then-fix like sub-project 4, no formal spec doc. Production task assignment was already correct/consistent — audit found no gaps there. Delivery workflow: counter_staff granted delivery assign/batch-assign/cancel/reattempt (user confirmed — same reasoning as settlements/expenses/pickup), `DeliveriesScreen` (the list + at-risk/overdue monitoring view — per-order status was already reachable via SaleDetail → DeliveryDetail) registered in `EmployeeOrdersStack` and its `isManager` checks widened via a new `canManageDeliveries` flag, a purpose-built `GET /deliveries/partners` endpoint added (matches the `staff-roster` narrow-endpoint precedent — `GET /users` stays owner/manager-only), and `DeliveryDetailScreen`'s "Fulfill from Stock" button fixed to match a backend permission it had missed. Convert-to-COD/Credit and Convert-to-Pickup deliberately left owner/manager-only — out of scope for what was confirmed, backend still restricts those too.
   - **Continuation — order-lifecycle simplification & delivery management — done (2026-09-01)**, full brainstorm → design doc (`docs/superpowers/specs/2026-09-01-order-lifecycle-delivery-management-design.md`) → 18-task implementation plan → build cycle, all 18 tasks complete and verified (Task 18 final regression: `verify-identity-roles.js` 10/10, unchanged; all 11 touched frontend files babel-transform clean; live end-to-end trace of one walk_in, one pickup, and one delivery order lifecycle at Test Loc, each including its negative/no-inline-action case). Built: `computeOrderStage()` (`server/utils/order-stage.js`) — a single order-type-aware `display_stage` + one-tap `nextAction`, computed fresh (never stored) and attached to `GET /sales` (list) and `GET /sales/:id` (detail), replacing the need for every screen to re-derive stage from `status`+`payment_status`+`pickup_status`+`delivery_status` independently; per-item `task_summary` on the detail route; `sales.vendor_name` (owner/manager-settable, owner-only readable — closed after an initial review found it leaking unstripped through 5 other routes plus an audit-log JSON blob, all fixed same task); `delivery_routes` table + create-or-find/list endpoints (a manual grouping tag for dispatch, explicitly not a routing/stop-sequencing algorithm — that's deferred, see below) and `deliveries.route_id`, wired into sale creation and surfaced on `GET /deliveries`; `RoutePicker` UI in `LogOrderScreen`/`QuickCheckoutScreen` for delivery orders (including pre-order-fulfilled-by-delivery, widened from the brief's literal `order_type==='delivery'` to match the screen's own `needsDelivery` flag); `OrderKanbanBoard` extracted from `DashboardScreen` (pure extraction) then given inline one-tap `nextAction` quick-actions (`api.advanceOrder`), and the counter_staff dashboard rebuilt onto it (grouping from the manager view, one-tap shortcuts from the counter view, with Call/WhatsApp restored on `OrderCard` after being caught missing from the flat-card era); `SaleDetailScreen` inline "Confirm Pickup"/"Mark Delivered" actions, with the pre-existing "Complete Order" button correctly hidden whenever the inline action covers the same case (the single-source-of-truth principle this whole redesign exists to enforce); `DeliveriesScreen` route-grouped/at-risk-first view with "select all in this route" batch assign; per-rider active-delivery-count in the assign picker; `delivery_load_checks` table + get/toggle endpoints (lazily created, one row per `sale_item`, the instant a delivery's checklist is first viewed) shared as one real-time state between counter staff (pre-dispatch loading) and the rider (self-verify before departure) — not two parallel lists — with the rider's own gate gated by delivery ownership.
   - **Explicitly deferred (per the design doc's own §9.2, named not silently dropped):** automated stop-sequencing/route optimization (needs a real external mapping API — same category of build-vs-defer decision as the WhatsApp Business API question under sub-project 4; §8's "route" stays a manual tag); rider capacity limits/auto-balancing assignment (confirmed out of scope at this shop's scale — plain per-rider count is the whole of what's built); `ProductionQueueScreen`'s own fixes/improvements (flagged for a future audit-and-fix pass at the user's request, not investigated this session — no known concrete issues, just un-audited); full CRUD/admin UI for `delivery_routes` (create-or-find is enough for now).
   - **Known minor gaps surfaced during the build/verification (accepted-as-is, not blocking, worth knowing before touching this code again):** `computeOrderStage()` has no defensive null-check on its input and silently falls back to the walk_in/pre_order ladder for an unrecognized `order_type` (reviewer-accepted — callers only ever pass real sale objects, and the fallback is the safe default). The detail route's per-item `task_summary` has a different shape than the pre-existing list/dashboard route's `task_summary` in the same file — a latent naming collision for whoever next builds a frontend consumer of both. `DashboardScreen`'s older "Orders Needing Attention" list still has its own separate one-tap mechanism (`advanceOrderStatus`) that doesn't consume `display_stage.nextAction` — two one-tap mechanisms now coexist in that file pending a future convergence pass. A pre-existing (not introduced by this work), still-open rough edge: an undispatched-but-ready delivery order shows "Complete Order" leading to a plain-language guard error rather than the correct action. `DeliveriesScreen`'s route-section builds aren't memoized. On the load checklist: `checked_by` is overwritten on every toggle including unchecking (a product nuance, not a bug); an item removed from a sale can orphan its checklist row, currently unreachable since nothing deletes a single `sale_item` without cascading the whole sale; the rider-side screen's "pull down to retry" error message has no pull-to-refresh actually wired, and its in-flight-toggle guard is global rather than per-row (silent no-op on a cross-row tap, no data risk); `GET /deliveries/:id` itself has no ownership restriction (pre-existing — only gates the UI button, not deep-link access; the checklist data stays properly 403'd). **Newly found in Task 18's live trace, not a prior task's finding:** `computeOrderStage()`'s delivery ladder checks the plain "Ready" branch (`status==='ready'` and `delivery_status` not yet `picked_up`/`in_transit`/`delivered`) before the "Out for Delivery" branch, so a delivery already assigned to a rider but not yet physically picked up from the shop still displays as plain "Ready" with no inline action rather than a distinct assigned/dispatched sub-state — cosmetic only (the real Assign → Pick Up → In Transit → Deliver flow all work correctly via `DeliveryDetailScreen`'s explicit buttons; nothing gets stuck), and it's exactly the code the plan's own Task 1 brief specified, not a slip introduced during implementation. Also noted, unrelated to this plan and out of scope to fix here: a fresh `walk_in` sale's initial `status` is decided by pre-existing (untouched) stock-check logic in `POST /sales` that only ever yields `'preparing'` or `'completed'`, never `'pending'` — so the walk_in ladder's "New" stage that `computeOrderStage()` defines is real and correctly computed whenever reached, but isn't actually reachable through today's normal order-creation path.
6. Attendance & shift management
7. Customer CRM (careful — live data, see constraint 1)
8. Catalog/inventory + reporting, consistent cost/margin enforcement
9. UI/UX design pass (frontend-design skill, once flows are correct)
10. Technical debt cleanup (schema drift, dual DB access layers, repo clutter) — opportunistic, not blocking

## Known structural debt — don't be surprised by this, and don't "fix" it accidentally as a side effect of other work

- `server/config/schema.sql` is **dead** — never loaded at runtime, don't trust it as ground truth. The real live schema is assembled by `server/config/database.js`'s `ensureCoreTables()` plus **116 idempotent `ensureColumn()` patch calls** run on every server boot, which have silently added/renamed columns and dropped CHECK constraints over time.
- **Two DB access layers coexist**, sometimes in the same route file: `database.js`'s `getDb()` shells out to the `psql` binary via `spawnSync` **per query** (sync, subprocess per call — a likely real performance cost); `database-async.js`'s `getDb()` is a proper async `pg.Pool`. Prefer the async layer for any new/touched code; don't expand use of the sync one.
- A `customers` table exists in `database.js` but is **dead/unused** — `server/routes/customers.js` reads/writes `users` (role='customer') exclusively. Don't assume the `customers` table holds real data, and don't touch it without checking first — leave it alone unless a specific task calls for cleaning it up.
- ~~Cash-register-open enforcement exists only client-side~~ — **fixed** (sub-project 3, see roadmap item 3 above): now hard-enforced server-side at every known cash-write site via `server/utils/register-guard.js`. Not yet checked: the customer self-order endpoint and the recurring-order processor — worth a quick audit before considering sub-project 3 fully closed.
- Two parallel dashboards coexist: `DashboardScreen.js` and `DashboardScreenV2.js`, toggled by a settings flag. Old one (V1) not yet removed.
- `products.estimated_cost` is currently **not** role-filtered — visible to every authenticated role today, unlike `materials.avg_purchase_cost` which is correctly owner-only. This directly contradicts the "no sensitive data to employees" requirement and needs fixing (sub-project 8; sub-project 2 concluded without touching this).
- The 4 live `employee` accounts stay on `employee` until the owner manually promotes each to `counter_staff`/`florist_staff` via the existing role-change UI (`UserFormScreen.js`, no forced migration) — expect `employee` to coexist with the two new roles indefinitely, not a transitional state to "clean up."
- `useIdleLock`'s touch-based activity signal has two known coverage gaps, both opportunistic follow-ups, not blocking: (1) it only reaches React Native `Modal` content on `QuickCheckoutScreen.js` and `LogOrderScreen.js`'s voice-note modals — the same gap exists in principle across ~27 other screens using `Modal` (`grep -rl "<Modal" app/src/screens/*.js`); `bumpActivity` is exported from `app/src/hooks/useIdleLock.js`, wire via `onTouchStart={bumpActivity}` on the modal's outer content view. (2) Was found live to also miss ALL mouse-wheel/trackpad scrolling on web (no touch event fires) — fixed centrally, not per-screen, via `window.addEventListener('wheel'/'scroll', bump)` in `RootNavigator.js` (2026-09-01), so this one's actually closed for web; native scroll already worked (a touch-scroll starts with a real touch-down).
  - The lock architecture itself changed 2026-09-01: locking used to unmount `MainNavigator` entirely (swapped for `LockedNavigator`), wiping any in-progress screen state — a staff member lost a whole order draft to this. Now `MainNavigator` stays mounted and the lock screen renders as an overlay on top of it (`RootNavigator.js`), keyed by `user?.id` so a genuine staff switch (different PIN) still force-remounts clean, but the same person re-entering their own PIN keeps all state. Side effect: screens don't lose React Navigation focus during a lock anymore, so anything relying on `useFocusEffect` to refresh on return needs its own explicit `locked`-transition refetch — done for `DashboardScreen.js`, not yet audited elsewhere (same "opportunistic, not exhaustive" caveat as the two gaps above).
- **`database-async.js`'s `bindParams()` has a real footgun**: it toggles an "inside a string" flag on every `'` character with no awareness of `--` SQL comments, so a single stray apostrophe inside a hand-written comment (e.g. `-- today's fix`) silently desyncs every `?` placeholder for the rest of that query — 500ing the endpoint with no error until someone live-tests that exact code path. This actually happened (`GET /production/my-tasks` broke for every caller from one comment). Fixed both the trigger and the root cause — `bindParams()` now skips `--` line comments before scanning — but it still has **no equivalent handling for `/* */` block comments** (none exist in the codebase's SQL today, so untested); avoid introducing one, or extend the skip logic first if you do. Moral: never put an apostrophe in a SQL comment in this layer, full stop — the parser can't tell it from a real string boundary.
- `GET /api/users` is intentionally owner/manager-only — it's the full account directory (owner/manager/customer included), too broad to open to staff just so one screen can list assignable employees. For any "who can I assign this to / call this person" need, use `GET /auth/staff-roster?location_id=` instead (already unauthenticated-safe by design, since `LockScreen` calls it pre-login) — minimal fields, pre-filtered to `employee`/`counter_staff`/`florist_staff`, location-scoped. `SaleDetailScreen`'s task-assign modal was switched to this pattern 2026-09-01; reuse it rather than widening `GET /users`' `authorize()` next time this need comes up elsewhere.
- **`POST /api/sales/customer-order` (the customer self-order endpoint) is completely broken — 500s on every call.** Found live by `verify-order-flows.js` during the 2026-09-04 regression pass; confirmed pre-existing (byte-identical between this branch's HEAD and the working tree at the time it was found, so not a regression from any change described above). Root cause: the `INSERT INTO sales` statement in that route (`server/routes/sales.js`, search `customer-order`) lists 24 columns but its `VALUES (...)` tuple has 25 entries (19 `?` placeholders against only 18 bound args) — an off-by-one that fails at the DB layer for every request. Two more real gaps surfaced by the same pass, same route/area, also **not yet fixed**: (1) `customer-order` itself has no role restriction beyond `authenticate` — any staff role can call it too, not just `customer` — currently masked by the 500 above, would need its own `authorize()` once the crash is fixed; (2) separately, staff-facing sale creation (`POST /sales`, used by POS/counter checkout) trusts a client-supplied `unit_price` on a real product, bypassing the discount-approval threshold entirely — a price-manipulation gap, not a crash. Customer self-ordering is a hard-constraint feature (see "What this is" above) — this needs its own fix pass, deliberately kept out of scope here per user direction (flag only, not fixed this session).

## Reference material

- `/Users/gauravbhatia/Downloads/files/petal-prd.md`, `build-plan.md` — conceptual reference only; wrong stack, don't follow literally.
- `PROGRESS.md` — history of what was built before this redesign.
- `ORDER_STATUS_AND_FLOW_ANALYSIS.md`, `docs/STATUS_LIFECYCLE_ALIGNMENT_2026-04-04.md`, `docs/ATTENDANCE_SHIFT_FLOW_ANALYSIS_2026-04-04.md` — prior gap-analysis docs, still relevant.
